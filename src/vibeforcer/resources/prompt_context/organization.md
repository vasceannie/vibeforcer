# Organization prompt context

- Preserve repository guardrails.
- Prefer deterministic hook outcomes over model-based approvals.
- Log every hook evaluation to JSONL so operators can replay decisions.
- Keep allow/deny reasons explicit, stable, and easy to grep.
